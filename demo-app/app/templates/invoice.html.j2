<!doctype html>
<title>Invoice — {{ title }}</title>
<h1>{{ title }}</h1>
<p>{{ body }}</p>
<hr>
<small>Checked-in template — not user-supplied (NOT_REACHABLE example). Uses fmt filter: {{ title | fmt("") }}</small>
